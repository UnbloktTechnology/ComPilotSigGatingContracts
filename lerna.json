{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "npmClient": "pnpm",
  "packages": ["contracts-sdk", "sig-gating-contracts"],
  "command": {
    "version": {
      "conventionalCommits": true,
      "message": "chore(release): version and release commit",
      "allowBranch": ["main"],
      "ignoreChanges": ["**/*.md", "**/*(\\.|-)test.ts", "**/*(\\.|-)test.js"]
    }
  }
}
